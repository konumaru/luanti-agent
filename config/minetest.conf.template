# Luanti Server Configuration Template
# Auto-generated configuration for AI agent experiments

# Server Settings
port = 30000
server_name = Luanti AI Agent Experimental Server
server_description = Luanti server for AI agent experiments
server_announce = false
max_users = 10

# World Generation - Fixed seed for reproducibility
mg_name = v7
mg_flags = caves, dungeons, light, decorations
mgv7_spflags = mountains, ridges, nofloatlands, caverns
fixed_map_seed = 12345678

# Game Settings
enable_damage = true
creative_mode = false
default_password = explaza
disallow_empty_password = true

# Time Settings
time_speed = 72

# Security
secure.enable_security = true
secure.trusted_mods = python_bot
secure.http_mods = python_bot

# Python Bot Configuration
python_bot.url = http://host.docker.internal:8000/next
python_bot.poll_interval = 0.2
python_bot.name = PyBot
python_bot.follow_player = konumaru
python_bot.follow_distance = 2
python_bot.follow_speed = 2

# MOB Settings (for future mob mods)
# Enable these when mob mods are installed
# enable_mob_spawning = true
# mob_difficulty = normal
