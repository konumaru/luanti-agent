[project]
name = "agent"
version = "0.1.0"
description = "Luanti AI Agent - Python client for agent control and observation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.31.0",
]
